## **問題4：入力履歴を保持するテキストボックス**

入力欄に文字を入力し、送信ボタンを押すたびに、前回入力した文字列を保持してコンソールに出力するコンポーネントを作成せよ。

---

### **条件**

(1) 現在の入力は `useState` で保持
(2) 前回の入力は `useRef` で保持
(3) TailwindCSS で入力欄を `p-2 border rounded`、ボタンを `p-2 bg-blue-500 text-white rounded`